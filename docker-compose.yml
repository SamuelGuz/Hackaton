services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    image: churn-oracle-backend
    container_name: churn-oracle-backend
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app/backend
    restart: unless-stopped
